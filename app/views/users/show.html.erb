<h1><%= @user.name %>のページ</h1>

<p>メールアドレス: <%= @user.email %></p>

<table>
  <thead>
    <tr>
      <th>お悩み</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.blogs.each do |blog| %>
      <tr>
      <%= blog.content %>
      </tr>
    <% end %>
  </tbody>
</table>




<%= link_to '編集', edit_user_path(@user.id) %>
